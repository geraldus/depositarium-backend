module Handler.Operator.Profile.View where

import           Import


getOperatorProfileViewR :: Handler Html
getOperatorProfileViewR = do
    error "dummy page which renders react components"
    error "show view user balance"
    error "restrict to operators only"
    error "get rur and pzm total balances"
    error "show balances"
    error "show paramining"
    error "provide Deposit and Withdrawal links"
